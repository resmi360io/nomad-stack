Build the Next.js app and start the local dev server.

Steps:
1. Run `npm run build` in D:\dev\nomad-stack using PowerShell. Wait for it to complete. Report any TypeScript or lint errors — if there are errors, stop and show them. If clean, confirm routes rendered.
2. Run `npm run dev` in D:\dev\nomad-stack as a background PowerShell task.
3. Read the task output file after a few seconds to confirm "Ready" appears.
4. Report the local URL (typically http://localhost:3000) and any warnings.
